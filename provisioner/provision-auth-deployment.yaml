apiVersion: apps/v1
kind: Deployment
metadata:
  name: provisioner-auth
spec:
  replicas: 1
  selector:
    matchLabels:
      app: provisioner-auth
  template:
    metadata:
      labels:
        app: provisioner-auth
    spec:
      containers:
        - name: provisioner-auth
          image: docker.io/folylyfo/provisioner-auth:09d1aa760bc7deeb20d6d0e9fcf7bbe222491098
          ports:
            - containerPort: 80
          envFrom:
            - secretRef:
                name: provisioner-env
