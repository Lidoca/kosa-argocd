apiVersion: apps/v1
kind: Deployment
metadata:
  name: provisioner-auth
spec:
  replicas: 1
  selector:
    matchLabels:
      app: provisioner-auth
  template:
    metadata:
      labels:
        app: provisioner-auth
    spec:
      containers:
        - name: provisioner-auth
          image: docker.io/folylyfo/provisioner-auth:e9d246ac47667c74a91c45e0ed282e4df50aa2e0
          ports:
            - containerPort: 80
          envFrom:
            - secretRef:
                name: provisioner-env
